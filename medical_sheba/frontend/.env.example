VITE_API_BASE_URL=/api
VITE_API_PROXY_TARGET=http://localhost:8000

# Local development can leave this blank because images use localhost:8000 in dev.
# If uploaded images/media are served from a different domain than the frontend in production,
# set this to that backend origin, for example:
# VITE_API_ORIGIN=https://api.example.com
